apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: website-downloads
  namespace: website
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-retain
  resources:
    requests:
      storage: 1Gi
