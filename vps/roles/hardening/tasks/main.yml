---
- name: Harden sshd
  ansible.builtin.import_tasks: sshd.yml
  tags: [sshd]

- name: Apply sysctl and unattended-upgrades
  ansible.builtin.import_tasks: sysctl_upgrades.yml
  tags: [sysctl, upgrades]

- name: Install and configure CrowdSec
  ansible.builtin.import_tasks: crowdsec.yml
  tags: [crowdsec]

- name: Configure ufw
  ansible.builtin.import_tasks: ufw.yml
  tags: [firewall]
