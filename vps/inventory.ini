# Example inventory — placeholders only, safe to commit.
#
# Real connection details (the VPS's public IP, SSH user/key) go in an
# untracked `inventory.local.ini` in this same directory, copied from this
# file. See vps/README.md.

[vps]
front-door ansible_host=<VPS_PUBLIC_IP> ansible_user=<SSH_USER>
