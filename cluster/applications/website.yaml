apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: website
  namespace: argocd
  annotations:
    # Wave 24: after the Gateway (5) and Longhorn storage exist.
    argocd.argoproj.io/sync-wave: "24"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  # infrastructure like every other app: it permits the declared namespace.yaml
  # (the apps project denies cluster-scoped resources and isn't adopted yet).
  project: infrastructure
  source:
    repoURL: https://github.com/alex-senger/homelab.git
    targetRevision: main
    path: apps/website
  destination:
    server: https://kubernetes.default.svc
    namespace: website
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
