apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: default-deny-egress
  namespace: minecraft
spec:
  endpointSelector: {}
  egress:
    # DNS
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    # Mojang session auth + skins + Modrinth/Spiget plugin fetches — all HTTPS.
    # world:443 only blocks non-443 C2 / cryptominer stratum ports / non-HTTPS exfil.
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
