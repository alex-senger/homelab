apiVersion: v1
kind: Namespace
metadata:
  name: wg-ingress
  labels:
    # WireGuard needs NET_ADMIN/SYS_MODULE and /dev/net/tun; baseline PSA forbids
    # both. Same precedent as infrastructure/longhorn/namespace.yaml.
    pod-security.kubernetes.io/enforce: privileged
