apiVersion: v1
kind: Namespace
metadata:
  name: wg-ingress
  labels:
    # WireGuard needs NET_ADMIN, which baseline PSA forbids. Same precedent as
    # infrastructure/longhorn/namespace.yaml.
    pod-security.kubernetes.io/enforce: privileged
