apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg
  namespace: databases
spec:
  instances: 1
  priorityClassName: homelab-critical
  # renovate: datasource=docker depName=ghcr.io/cloudnative-pg/postgresql
  imageName: ghcr.io/cloudnative-pg/postgresql:18.6-202609210816-minimal-trixie

  storage:
    size: 5Gi
    storageClass: longhorn-retain

  # WAL archiver + backup plugin, backed by the Garage ObjectStore.
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: pg-backups
        # One archive path per PG major: pg_upgrade resets the system ID and
        # timeline, so PG18 WAL would clobber PG17's in a shared path. Bump
        # together with the major version in imageName.
        serverName: pg-18

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi

  # Declarative role management: create/own the authelia role, password from
  # the ESO-materialised basic-auth secret.
  managed:
    roles:
      - name: authelia
        ensure: present
        login: true
        passwordSecret:
          name: authelia-db
      - name: nextcloud
        ensure: present
        login: true
        passwordSecret:
          name: nextcloud-db
